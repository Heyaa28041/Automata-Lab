<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ATM Automata — Finite State Machine</title>
    <link href="https://fonts.googleapis.com/css2?family=Space+Mono:wght@400;700&family=Syne:wght@400;600;800&display=swap" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/vis-network/9.1.2/dist/vis-network.min.js"></script>
    <style>
        :root {
            --bg: #0a0a0f;
            --surface: #111118;
            --surface2: #1a1a24;
            --border: #2a2a3a;
            --border-bright: #3d3d55;
            --text: #e8e8f0;
            --muted: #6b6b85;
            --accent: #7c3aed;
            --accent2: #06b6d4;
            --green: #10b981;
            --red: #f43f5e;
            --glow-cyan: rgba(6, 182, 212, 0.2);
        }

        * { box-sizing: border-box; margin: 0; padding: 0; }

        body {
            background: var(--bg);
            color: var(--text);
            font-family: 'Space Mono', monospace;
            min-height: 100vh;
            padding: 32px 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .container {
            width: 100%;
            max-width: 1100px;
            position: relative;
            z-index: 1;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            padding-bottom: 28px;
            border-bottom: 1px solid var(--border);
            margin-bottom: 32px;
        }

        .title-block h1 {
            font-family: 'Syne', sans-serif;
            font-weight: 800;
            font-size: 2.4rem;
            letter-spacing: -1px;
            line-height: 1;
            background: linear-gradient(135deg, var(--accent2) 0%, var(--accent) 60%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .subtitle {
            font-size: 0.7rem;
            color: var(--muted);
            margin-top: 6px;
            letter-spacing: 3px;
            text-transform: uppercase;
        }

        .meta-info {
            text-align: right;
            font-size: 0.65rem;
            color: var(--muted);
            line-height: 1.8;
        }

        .tabs {
            display: flex;
            gap: 4px;
            margin-bottom: 28px;
            border-bottom: 1px solid var(--border);
            padding-bottom: 0;
        }

        .tab-btn {
            background: none;
            border: none;
            color: var(--muted);
            font-family: 'Space Mono', monospace;
            font-size: 0.7rem;
            padding: 10px 18px;
            cursor: pointer;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            border-bottom: 2px solid transparent;
            transition: all 0.2s;
            position: relative;
            bottom: -1px;
        }

        .tab-btn:hover { color: var(--text); }
        .tab-btn.active { color: var(--accent2); border-bottom-color: var(--accent2); }

        .tab-content { display: none; }
        .tab-content.active {
            display: block;
            animation: fadeUp 0.25s ease-out;
        }

        @keyframes fadeUp {
            from { opacity: 0; transform: translateY(8px); }
            to   { opacity: 1; transform: translateY(0); }
        }

        .panel {
            border: 1px solid var(--border);
            padding: 22px;
            margin-bottom: 18px;
            position: relative;
            background: var(--surface);
            border-radius: 8px;
            overflow: hidden;
        }

        .panel::after {
            content: '';
            position: absolute;
            top: 0; left: 0; right: 0;
            height: 2px;
            background: linear-gradient(90deg, var(--accent), var(--accent2), transparent);
            opacity: 0.5;
        }

        .panel-label {
            font-size: 0.6rem;
            letter-spacing: 2px;
            text-transform: uppercase;
            color: var(--muted);
            margin-bottom: 16px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .panel-label::before { content: '//'; color: var(--accent); }

        .two-col {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 18px;
        }

        .btn {
            background: var(--surface2);
            border: 1px solid var(--border);
            color: var(--text);
            font-family: 'Space Mono', monospace;
            font-size: 0.65rem;
            padding: 10px 14px;
            cursor: pointer;
            border-radius: 4px;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: all 0.15s;
        }

        .btn:hover { border-color: var(--accent2); color: var(--accent2); }
        .btn:active { transform: scale(0.98); }

        .btn-primary { border-color: var(--accent); color: var(--accent); }
        .btn-primary:hover { background: var(--accent); color: white; }

        .btn-danger { border-color: var(--red); color: var(--red); }
        .btn-danger:hover { background: var(--red); color: white; }

        .field-label {
            font-size: 0.6rem;
            color: var(--muted);
            text-transform: uppercase;
            letter-spacing: 1.5px;
            margin-bottom: 6px;
            display: block;
        }

        .mini-input {
            width: 100%;
            background: var(--surface2);
            border: 1px solid var(--border);
            color: var(--text);
            font-family: 'Space Mono', monospace;
            font-size: 0.75rem;
            padding: 10px 14px;
            border-radius: 4px;
            margin-bottom: 14px;
        }

        .mini-input:focus { outline: none; border-color: var(--accent); }

        input[type="password"] {
            background: var(--surface2);
            border: 1px solid var(--border);
            color: var(--accent2);
            padding: 10px 14px;
            font-family: 'Space Mono', monospace;
            font-size: 0.75rem;
            border-radius: 4px;
            letter-spacing: 6px;
            text-align: center;
            width: 110px;
        }

        input[type="password"]:focus { outline: none; border-color: var(--accent); }

        .phase-result {
            padding: 14px;
            border: 1px solid var(--border);
            margin-top: 14px;
            font-size: 0.72rem;
            background: var(--surface2);
            border-radius: 4px;
            color: var(--muted);
        }

        .phase-result.accept {
            border-color: rgba(16, 185, 129, 0.4);
            background: rgba(16, 185, 129, 0.06);
            color: var(--green);
        }

        .phase-result.reject {
            border-color: rgba(244, 63, 94, 0.4);
            background: rgba(244, 63, 94, 0.06);
            color: var(--red);
        }

        /* ── GRAPH CONTAINERS — white bg so vis labels are always readable ── */
        .graph-container {
            border-radius: 6px;
            border: 1px solid var(--border);
            overflow: hidden;
            background: #ffffff;  /* white so node text is always contrast-safe */
        }

        #graph        { height: 380px; }
        #tmGraph      { height: 300px; }

        /* ── STATS ── */
        .stats-panel {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 12px;
            margin-bottom: 24px;
        }

        .stat-box {
            background: var(--surface);
            border: 1px solid var(--border);
            padding: 12px 16px;
            border-radius: 6px;
        }

        .stat-label {
            font-size: 0.55rem;
            color: var(--muted);
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 6px;
        }

        .stat-value {
            font-size: 1.1rem;
            color: var(--accent2);
            font-weight: 700;
            font-family: 'Space Mono', monospace;
        }

        /* ── STACK ── */
        .stack-item {
            border: 1px solid var(--border);
            padding: 10px;
            min-width: 55px;
            text-align: center;
            background: rgba(6, 182, 212, 0.05);
            color: var(--accent2);
            font-weight: bold;
            font-size: 0.75rem;
            border-radius: 4px;
            margin-bottom: 4px;
        }

        /* ── TAPE ── */
        .tape-row {
            display: flex;
            gap: 4px;
            flex-wrap: wrap;
            margin-bottom: 20px;
        }

        .tape-cell {
            min-width: 44px;
            height: 44px;
            display: flex;
            align-items: center;
            justify-content: center;
            border: 1px solid var(--border);
            border-radius: 4px;
            font-size: 0.7rem;
            background: var(--surface2);
            color: var(--muted);
            transition: all 0.2s;
        }

        .tape-cell.active {
            background: var(--accent);
            border-color: var(--accent);
            color: white;
            box-shadow: 0 0 10px rgba(124,58,237,0.4);
        }

        #transitionLog {
            font-size: 0.65rem;
            color: var(--green);
            line-height: 1.7;
            max-height: 120px;
            overflow-y: auto;
            padding-top: 10px;
            border-top: 1px solid var(--border);
            margin-top: 12px;
            white-space: pre-wrap;
        }

        #errorToast {
            position: fixed;
            top: 20px;
            left: 50%;
            transform: translateX(-50%) translateY(-80px);
            background: var(--red);
            color: white;
            font-family: 'Space Mono', monospace;
            font-size: 0.7rem;
            padding: 12px 28px;
            border-radius: 4px;
            z-index: 9999;
            transition: transform 0.3s ease;
            letter-spacing: 2px;
        }

        #errorToast.show { transform: translateX(-50%) translateY(0); }

        #cfgTree {
            font-size: 0.72rem;
            color: var(--accent2);
            line-height: 1.9;
            white-space: pre-wrap;
        }

        @media (max-width: 700px) {
            .two-col { grid-template-columns: 1fr; }
            .title-block h1 { font-size: 1.8rem; }
            .stats-panel { grid-template-columns: 1fr 1fr; }
        }
    </style>
</head>
<body>

<div id="errorToast">AUTH_FAILURE — TRAP_STATE</div>

<div class="container">
    <header>
        <div class="title-block">
            <h1>AUTOMATA LAB</h1>
            <div class="subtitle">Theory of Computation Interactive Simulator</div>
        </div>
        <div class="meta-info">
            <div>DFA · Regex · PDA · TM · NFA · CFG</div>
            <div style="margin-top:4px;">Pakhi Mittal (24BYB0062) | Priyasha Das (24BYB0055)</div>
            <div>Theory of Computation | Formal Languages</div>
        </div>
    </header>

    <div class="stats-panel">
        <div class="stat-box">
            <div class="stat-label">Current FSM State</div>
            <div class="stat-value" id="statCurrentState">Q0</div>
        </div>
        <div class="stat-box">
            <div class="stat-label">State Transitions</div>
            <div class="stat-value" id="statTransitions">0</div>
        </div>
        <div class="stat-box">
            <div class="stat-label">Stack Size</div>
            <div class="stat-value" id="statStackSize">0</div>
        </div>
        <div class="stat-box">
            <div class="stat-label">System Mode</div>
            <div class="stat-value" id="statMode">STANDBY</div>
        </div>
    </div>

    <nav class="tabs">
        <button class="tab-btn active" onclick="switchTab('interactive', this)">01 · DFA Core</button>
        <button class="tab-btn" onclick="switchTab('phase2', this)">02 · Regex Match</button>
        <button class="tab-btn" onclick="switchTab('phase3', this)">03 · PDA Stack</button>
        <button class="tab-btn" onclick="switchTab('phase4', this)">04 · TM Decider</button>
        <button class="tab-btn" onclick="switchTab('phase5', this)">05 · NFA Sim</button>
        <button class="tab-btn" onclick="switchTab('phase6', this)">06 · CFG Parse</button>
    </nav>

    <!-- ═══ TAB 1: DFA ═══ -->
    <div id="interactive" class="tab-content active">
        <div class="two-col">
            <div class="panel">
                <div class="panel-label">Input Sequence</div>
                <p style="font-size:0.7rem;color:var(--muted);margin-bottom:12px;line-height:1.5;">Enter space-separated symbols to simulate the DFA. Valid: card, pin, op, withdraw.</p>
                <label class="field-label">Symbols (space-separated)</label>
                <input class="mini-input" id="symbolInput" value="card pin op withdraw">
                <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:14px;">
                    <button class="btn" onclick="runDFASequence()">▶ Run</button>
                    <button class="btn btn-primary" onclick="onResetTransition()">⟳ Reset</button>
                </div>
                <label class="field-label">Current State</label>
                <div id="currentStateDisplay" style="font-size:0.85rem;font-weight:600;color:var(--accent2);padding:12px;background:rgba(6,182,212,0.1);border:1px solid rgba(6,182,212,0.3);border-radius:4px;text-align:center;">Q0</div>
                <div id="transitionLog">δ(start, ε) → q0</div>
            </div>
            <div class="panel">
                <div class="panel-label">Execution Path</div>
                <div id="pathVisualization" style="font-size:0.7rem;color:var(--muted);padding:14px;background:var(--surface2);border-radius:4px;min-height:160px;line-height:2;font-family:'Space Mono';white-space:pre-wrap;">Step-by-step path will appear here</div>
                <div id="resultBox" class="phase-result" style="min-height:50px;display:flex;align-items:center;justify-content:center;text-align:center;">Waiting for input</div>
            </div>
        </div>
        <div class="panel">
            <div class="panel-label">Transition Matrix</div>
            <div style="overflow-x:auto;" id="matrixContainer"></div>
        </div>
        <div class="panel">
            <div class="panel-label">FSM State Diagram</div>
            <div id="graph" class="graph-container"></div>
        </div>
    </div>

    <!-- ═══ TAB 2: REGEX ═══ -->
    <div id="phase2" class="tab-content">
        <div class="two-col">
            <div class="panel">
                <div class="panel-label">Pattern & Input</div>
                <label class="field-label">Regex Pattern</label>
                <input class="mini-input" id="regexPattern" value="(a|b)*">
                <label class="field-label">Test String</label>
                <input class="mini-input" id="regexInputSeq" placeholder="abab">
                <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;">
                    <button class="btn" onclick="onRegexTransition()">Test</button>
                    <button class="btn btn-primary" onclick="setRegexExample2()">Example</button>
                </div>
                <div id="regexResult" class="phase-result">Ready for input</div>
            </div>
            <div class="panel">
                <div class="panel-label">Common Patterns</div>
                <div style="font-size:0.7rem;color:var(--muted);line-height:2;">
<span style="color:var(--accent2);">a*</span>       — Zero or more 'a'
<span style="color:var(--accent2);">a+</span>       — One or more 'a'
<span style="color:var(--accent2);">(a|b)*</span>  — Alternation
<span style="color:var(--accent2);">a.b</span>      — Any char between
<span style="color:var(--accent2);">^a$</span>      — Anchored match
<span style="color:var(--accent2);">[a-z]</span>   — Character class
<span style="color:var(--accent2);">\d</span>       — Digit
<span style="color:var(--accent2);">\s</span>       — Whitespace
                </div>
            </div>
        </div>
    </div>

    <!-- ═══ TAB 3: PDA ═══ -->
    <div id="phase3" class="tab-content">
        <div class="two-col">
            <div class="panel">
                <div class="panel-label">Stack Operations</div>
                <label class="field-label">Push Symbol</label>
                <input class="mini-input" id="pdaSymbol" placeholder="A | B | C | $" style="text-transform:uppercase;">
                <label class="field-label">Stack Depth</label>
                <input class="mini-input" id="stackDepth" type="number" value="0" readonly>
                <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;">
                    <button class="btn btn-primary" onclick="onStackTransition('PUSH')">Push</button>
                    <button class="btn btn-danger" onclick="onStackTransition('POP')">Pop</button>
                </div>
                <div id="pdaInstant" style="margin-top:14px;font-size:0.7rem;color:var(--accent2);padding:10px;background:rgba(6,182,212,0.05);border:1px solid rgba(6,182,212,0.15);border-radius:4px;">Stack: empty</div>
            </div>
            <div class="panel">
                <div class="panel-label">Stack Visualization</div>
                <div id="pdaStackContainer" style="display:flex;flex-direction:column-reverse;gap:4px;min-height:200px;padding:10px;background:rgba(10,10,15,0.4);border-radius:4px;border:1px dashed var(--border);">
                    <div style="text-align:center;color:var(--muted);font-size:0.7rem;">Empty</div>
                </div>
            </div>
        </div>
    </div>

    <!-- ═══ TAB 4: TM ═══ -->
    <div id="phase4" class="tab-content">
        <div class="panel">
            <div class="panel-label">Turing Machine Simulator</div>
            <div style="display:grid;grid-template-columns:1fr 1fr;gap:18px;margin-bottom:20px;">
                <div>
                    <label class="field-label">Input Tape</label>
                    <input class="mini-input" id="tmInput" value="abab">
                    <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;">
                        <button class="btn" onclick="runTM()">Run TM</button>
                        <button class="btn btn-primary" onclick="resetTM()">Reset</button>
                    </div>
                </div>
                <div>
                    <label class="field-label">Current State</label>
                    <div id="tmState" style="font-size:0.85rem;font-weight:600;color:var(--accent2);padding:12px;background:rgba(6,182,212,0.1);border:1px solid rgba(6,182,212,0.3);border-radius:4px;">q0</div>
                </div>
            </div>
            <label class="field-label">Tape (Head Position Highlighted)</label>
            <div id="tmTape" class="tape-row" style="padding:12px;background:rgba(10,10,15,0.4);border-radius:4px;border:1px dashed var(--border);margin-bottom:20px;"></div>
            <div style="display:grid;grid-template-columns:1fr 1fr;gap:18px;margin-bottom:20px;">
                <div>
                    <label class="field-label">Step Log</label>
                    <div id="tmLog" style="font-size:0.65rem;color:var(--muted);padding:10px;background:var(--surface2);border-radius:4px;min-height:200px;max-height:280px;overflow-y:auto;white-space:pre-wrap;line-height:1.6;">Ready to execute</div>
                </div>
                <div>
                    <label class="field-label">Result</label>
                    <div id="tmResult" class="phase-result" style="min-height:200px;display:flex;align-items:center;justify-content:center;text-align:center;">Awaiting execution</div>
                </div>
            </div>
            <label class="field-label">TM State Diagram</label>
            <div id="tmGraph" class="graph-container" style="height:300px;"></div>
        </div>
    </div>

    <!-- ═══ TAB 5: NFA ═══ -->
    <div id="phase5" class="tab-content">
        <div class="two-col">
            <div class="panel">
                <div class="panel-label">Non-deterministic FA</div>
                <label class="field-label">Input String</label>
                <input class="mini-input" id="nfaInput" value="aab">
                <label class="field-label">Current State Set</label>
                <div id="nfaCurrentStates" style="font-size:0.7rem;color:var(--accent2);padding:10px;background:rgba(6,182,212,0.08);border:1px solid rgba(6,182,212,0.2);border-radius:4px;margin-bottom:14px;">{q0}</div>
                <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;">
                    <button class="btn" onclick="simulateNFA()">Simulate</button>
                    <button class="btn btn-primary" onclick="resetNFA()">Reset</button>
                </div>
                <div id="nfaResult" class="phase-result">Ready</div>
            </div>
            <div class="panel">
                <div class="panel-label">NFA Definition</div>
                <div style="font-size:0.7rem;color:var(--muted);line-height:1.9;padding:10px;background:var(--surface2);border-radius:4px;">
<strong style="color:var(--accent);">States:</strong>   q0, q1, q2
<strong style="color:var(--accent);">Start:</strong>    q0
<strong style="color:var(--accent);">Accept:</strong>   q2

<strong style="color:var(--accent);">Transitions:</strong>
δ(q0, a) → {q0, q1}
δ(q0, b) → {q0}
δ(q1, b) → {q2}
δ(q2, ε) → ∅
                </div>
            </div>
        </div>
    </div>

    <!-- ═══ TAB 6: CFG ═══ -->
    <div id="phase6" class="tab-content">
        <div class="two-col">
            <div class="panel">
                <div class="panel-label">Grammar Parser</div>
                <label class="field-label">Input String</label>
                <input class="mini-input" id="cfgInput" value="aabb">
                <label class="field-label">Grammar Type</label>
                <select style="width:100%;padding:8px;background:var(--surface2);border:1px solid var(--border);color:var(--text);border-radius:4px;margin-bottom:14px;font-family:'Space Mono';font-size:0.7rem;">
                    <option>Balanced: a^n b^n</option>
                    <option>Palindromes</option>
                    <option>Nested: a^n b^n c^n</option>
                </select>
                <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;">
                    <button class="btn" onclick="parseCFG()">Parse</button>
                    <button class="btn btn-primary" onclick="resetCFG()">Clear</button>
                </div>
                <div id="cfgResult" class="phase-result">Ready for input</div>
            </div>
            <div class="panel">
                <div class="panel-label">Derivation Tree</div>
                <pre id="cfgDerivation" style="font-size:0.65rem;color:var(--muted);padding:10px;background:var(--surface2);border-radius:4px;min-height:240px;overflow-y:auto;line-height:1.7;font-family:'Space Mono';">S
├─ aA
│  ├─ aA
│  │  └─ ε
│  └─ Bb
└─ b</pre>
            </div>
        </div>
    </div>
</div>

<!-- GUIDE -->
<div class="container" style="margin-top:40px;">
    <div class="panel" style="background:rgba(6,182,212,0.03);border-color:rgba(6,182,212,0.15);">
        <div class="panel-label" style="margin-bottom:20px;">Navigation Guide</div>
        <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:20px;font-size:0.65rem;color:var(--muted);line-height:1.8;">
            <div><span style="color:var(--accent);font-weight:700;">01 · DFA Core</span><br>Simulate the ATM DFA. Enter symbol sequences and trace state transitions step by step.<br><em style="color:var(--accent2);">Try: card pin op withdraw</em></div>
            <div><span style="color:var(--accent);font-weight:700;">02 · Regex Match</span><br>Test regular expressions against input strings. Standard JS regex syntax supported.<br><em style="color:var(--accent2);">Try: Pattern (a|b)*, String abab</em></div>
            <div><span style="color:var(--accent);font-weight:700;">03 · PDA Stack</span><br>Push and pop symbols onto the stack. Visualizes the Γ stack of a Pushdown Automaton.<br><em style="color:var(--accent2);">Try: Push A, B, C then pop</em></div>
            <div><span style="color:var(--accent);font-weight:700;">04 · TM Decider</span><br>Turing Machine that scans input tape and halts in accept or reject state.<br><em style="color:var(--accent2);">Try: Input abbab</em></div>
            <div><span style="color:var(--accent);font-weight:700;">05 · NFA Sim</span><br>NFA with parallel state sets. Watch multiple active states evolve on each input symbol.<br><em style="color:var(--accent2);">Try: Input abb</em></div>
            <div><span style="color:var(--accent);font-weight:700;">06 · CFG Parse</span><br>Checks if a string belongs to a^n b^n grammar with derivation tree output.<br><em style="color:var(--accent2);">Try: aabb</em></div>
        </div>
        <div style="border-top:1px solid var(--border);padding-top:16px;margin-top:16px;font-size:0.65rem;color:var(--muted);">
            <strong style="color:var(--accent);">Keyboard Shortcuts:</strong>&nbsp;&nbsp;
            <span style="color:var(--accent2);">1–6</span> Switch tabs &nbsp;·&nbsp;
            <span style="color:var(--accent2);">R</span> Reset current tab
        </div>
    </div>
</div>

<script>
// ── CONSTANTS ─────────────────────────────────────────────────
const STATES   = ["q0","q1","q2","q3","q4"];
const ALPHABET = ["card","pin","op","withdraw"];
const δ = {
    q0:{ card:"q1" },
    q1:{ pin:"q2" },
    q2:{ op:"q3" },
    q3:{ withdraw:"q4" },
    q4:{ card:"q1" }
};

// ── GRAPH SHARED STYLE ────────────────────────────────────────
// White background on graph divs means we use DARK fonts on nodes.
// Active node = purple fill, white text.
// Normal node = light grey fill, dark text.
// Accept node = green fill, white text.
const nodeDefaults = (id, isActive, isAccept) => ({
    id,
    label: id.toUpperCase(),
    shape: (id === "q4" || id === "qa") ? "diamond" : "ellipse",
    color: {
        background: isActive  ? "#7c3aed"
                  : isAccept  ? "#10b981"
                  : "#e8e8f0",
        border:     isActive  ? "#5b21b6"
                  : isAccept  ? "#059669"
                  : "#9ca3af",
        highlight: { background:"#7c3aed", border:"#5b21b6" },
        hover:     { background:"#a78bfa", border:"#7c3aed" }
    },
    font: {
        color:     (isActive || isAccept) ? "#ffffff" : "#111827",
        size:      13,
        face:      "Space Mono",
        bold:      true,
        strokeWidth: 0
    },
    borderWidth:      isActive ? 3 : 1,
    shadow: isActive  ? { enabled:true, color:"rgba(124,58,237,0.5)", size:12, x:0, y:0 } : false
});

const edgeDefaults = {
    color:  { color:"#6b7280", highlight:"#7c3aed", hover:"#a78bfa" },
    font:   { color:"#374151", size:11, face:"Space Mono", background:"#f3f4f6", strokeWidth:0 },
    arrows: { to:{ enabled:true, scaleFactor:0.7 } },
    smooth: { type:"curvedCW", roundness:0.25 }
};

const graphOptions = {
    edges:   edgeDefaults,
    physics: { barnesHut:{ gravitationalConstant:-3000, centralGravity:0.4 } },
    interaction: { hover:true }
};

// ── STATE ─────────────────────────────────────────────────────
let currentState   = "q0";
let network        = null;
let tmNetwork      = null;
let stack          = [];
let transitionCount = 0;

// ── TABS ──────────────────────────────────────────────────────
function switchTab(id, btn) {
    document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('active'));
    document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
    document.getElementById(id).classList.add('active');
    if (btn) btn.classList.add('active');
    if (id === 'interactive') { if (!network) buildMainGraph(); else network.redraw(); }
    if (id === 'phase4') drawTMGraph('q0');
    updateStats();
}

// ── STATS ─────────────────────────────────────────────────────
function updateStats() {
    document.getElementById('statCurrentState').textContent = currentState.toUpperCase();
    document.getElementById('statTransitions').textContent  = transitionCount;
    document.getElementById('statStackSize').textContent    = stack.length;
    const modes = { q0:'INIT', q1:'VERIFY', q2:'PROCESS', q3:'ADVANCE', q4:'ACCEPT' };
    document.getElementById('statMode').textContent = modes[currentState] || 'RUNNING';
}

// ── DFA ───────────────────────────────────────────────────────
function runDFASequence() {
    const input = document.getElementById('symbolInput').value.trim();
    if (!input) return;
    const symbols = input.split(/\s+/).filter(Boolean);
    let state = 'q0', path = ['q0'], steps = ['START: q0'];

    for (let i = 0; i < symbols.length; i++) {
        const sym = symbols[i];
        const next = δ[state]?.[sym];
        if (next) {
            steps.push(`Step ${i+1}: δ(${state}, ${sym}) → ${next}`);
            state = next; path.push(state); transitionCount++;
        } else {
            steps.push(`Step ${i+1}: δ(${state}, ${sym}) → REJECT (no transition)`);
            state = 'REJECT'; break;
        }
    }

    currentState = (state === 'REJECT') ? 'q0' : state;
    document.getElementById('currentStateDisplay').innerText = state.toUpperCase();
    document.getElementById('transitionLog').innerText = steps.join('\n');
    document.getElementById('pathVisualization').innerText =
        `Path: ${path.join(' → ')}\nSteps: ${symbols.length}\nFinal: ${state}`;

    const rb = document.getElementById('resultBox');
    if (state === 'q4') {
        rb.className = 'phase-result accept';
        rb.innerText = '✓ ACCEPTED — Reached final state q4';
    } else if (state === 'REJECT') {
        rb.className = 'phase-result reject';
        rb.innerText = '✗ REJECTED — Invalid transition';
    } else {
        rb.className = 'phase-result';
        rb.innerText = `⊕ Halted at ${state}`;
    }

    syncTable(); buildMainGraph(); updateStats();
}

function onResetTransition() {
    currentState = 'q0'; transitionCount = 0;
    document.getElementById('currentStateDisplay').innerText = 'Q0';
    document.getElementById('transitionLog').innerText = 'δ(start, ε) → q0';
    document.getElementById('pathVisualization').innerText = 'Step-by-step path will appear here';
    const rb = document.getElementById('resultBox');
    rb.className = 'phase-result'; rb.innerText = 'Waiting for input';
    document.getElementById('symbolInput').value = '';
    syncTable(); buildMainGraph(); updateStats();
}

function syncTable() {
    const cols = ALPHABET;
    let h = '<table style="width:100%;border-collapse:collapse;font-size:0.65rem;">';
    h += '<thead><tr>';
    h += '<th style="border:1px solid var(--border);padding:8px 12px;background:var(--surface2);color:var(--muted);text-transform:uppercase;">State</th>';
    cols.forEach(c => h += `<th style="border:1px solid var(--border);padding:8px 12px;background:var(--surface2);color:var(--muted);text-transform:uppercase;">${c}</th>`);
    h += '</tr></thead><tbody>';
    STATES.forEach(s => {
        const active = s === currentState;
        const rowStyle = active
            ? 'background:rgba(6,182,212,0.08);'
            : '';
        h += `<tr style="${rowStyle}">`;
        const tdStyle = active
            ? 'border:1px solid rgba(6,182,212,0.3);padding:8px 12px;text-align:center;color:var(--accent2);'
            : 'border:1px solid var(--border);padding:8px 12px;text-align:center;color:var(--muted);';
        h += `<td style="${tdStyle}font-weight:${active?700:400};"><strong>${s}</strong></td>`;
        cols.forEach(sym => {
            const d = δ[s]?.[sym] || '—';
            h += `<td style="${tdStyle}">${d}</td>`;
        });
        h += '</tr>';
    });
    h += '</tbody></table>';
    document.getElementById('matrixContainer').innerHTML = h;
}

// ── MAIN GRAPH ────────────────────────────────────────────────
function buildMainGraph() {
    const container = document.getElementById('graph');
    const nodes = STATES.map(s => nodeDefaults(s, s === currentState, s === 'q4'));
    const edges = [];
    for (const [src, targets] of Object.entries(δ))
        for (const [sym, dst] of Object.entries(targets))
            edges.push({ from:src, to:dst, label:sym, ...edgeDefaults });

    if (network) network.destroy();
    network = new vis.Network(container,
        { nodes: new vis.DataSet(nodes), edges: new vis.DataSet(edges) },
        graphOptions
    );
}

// ── REGEX ─────────────────────────────────────────────────────
function onRegexTransition() {
    const pattern = document.getElementById('regexPattern').value.trim();
    const input   = document.getElementById('regexInputSeq').value.trim();
    const res     = document.getElementById('regexResult');
    try {
        const ok = new RegExp(`^${pattern}$`).test(input);
        res.className = `phase-result ${ok ? 'accept' : 'reject'}`;
        res.innerText = ok
            ? `✓ ACCEPTED — "${input}" matches /${pattern}/`
            : `✗ REJECTED — "${input}" does not match /${pattern}/`;
    } catch(e) {
        res.className = 'phase-result reject';
        res.innerText = `✗ Invalid regex: ${e.message}`;
    }
}

function setRegexExample2() {
    document.getElementById('regexInputSeq').value = 'bab';
    onRegexTransition();
}

// ── PDA ───────────────────────────────────────────────────────
function onStackTransition(type) {
    const sym = document.getElementById('pdaSymbol').value.trim().toUpperCase();
    if (type === 'PUSH' && sym) stack.push(sym);
    else if (type === 'POP' && stack.length) stack.pop();
    document.getElementById('stackDepth').value = stack.length;
    document.getElementById('pdaSymbol').value  = '';
    document.getElementById('pdaInstant').innerText = `Stack: [${stack.join(', ') || 'empty'}]`;
    const host = document.getElementById('pdaStackContainer');
    host.innerHTML = stack.length
        ? [...stack].reverse().map(s =>
            `<div class="stack-item" style="width:100%;text-align:center;">${s}</div>`).join('')
        : '<div style="text-align:center;color:var(--muted);font-size:0.7rem;padding:60px 0;">Stack is empty</div>';
    updateStats();
}

// ── TM ────────────────────────────────────────────────────────
function runTM() {
    const input = document.getElementById('tmInput').value.trim() || 'abab';
    const tape  = input.split('');
    let pos = 0, state = 'q0', steps = [`Step 0: state=q0, head=0`];

    while (pos < tape.length) {
        const sym = tape[pos];
        steps.push(`Step ${pos+1}: read '${sym}' at pos=${pos}, state=${state}`);
        if (sym === 'a')      state = 'q1';
        else if (sym === 'b') state = state === 'q1' ? 'q0' : 'q0';
        else { state = 'reject'; steps.push('  → invalid symbol → REJECT'); break; }
        pos++;
        steps.push(`  → state=${state}, head=${pos}`);
    }

    if (state !== 'reject') state = (state === 'q0') ? 'qa' : 'reject';
    steps.push(`\nHalt: ${state.toUpperCase()}`);

    document.getElementById('tmState').innerText = state.toUpperCase();
    document.getElementById('tmLog').innerText   = steps.join('\n');
    renderTapeTM(tape, pos - 1);
    const r = document.getElementById('tmResult');
    r.className = `phase-result ${state === 'qa' ? 'accept' : 'reject'}`;
    r.innerText = state === 'qa'
        ? '✓ ACCEPTED — Halted in q_accept'
        : '✗ REJECTED — Halted in q_reject';
    drawTMGraph(state);
}

function resetTM() {
    document.getElementById('tmInput').value   = 'abab';
    document.getElementById('tmState').innerText = 'q0';
    document.getElementById('tmLog').innerText  = 'Ready to execute';
    document.getElementById('tmResult').className = 'phase-result';
    document.getElementById('tmResult').innerText = 'Awaiting execution';
    document.getElementById('tmTape').innerHTML  = '';
    drawTMGraph('q0');
}

function renderTapeTM(tape, activePos) {
    document.getElementById('tmTape').innerHTML = tape.map((s, i) =>
        `<div class="tape-cell${i === activePos ? ' active' : ''}">${s}</div>`
    ).join('');
}

function drawTMGraph(active = 'q0') {
    const container = document.getElementById('tmGraph');
    if (!container) return;
    // Use same white-bg approach; reuse nodeDefaults logic inline
    const tmStates = [
        { id:'q0', active: active==='q0', accept: false },
        { id:'q1', active: active==='q1', accept: false },
        { id:'qa', active: active==='qa', accept: true  },
    ];
    const nodes = tmStates.map(s => nodeDefaults(s.id, s.active, s.accept));
    const edges = [
        { from:'q0', to:'q1', label:'a', ...edgeDefaults },
        { from:'q0', to:'q0', label:'b', ...edgeDefaults, smooth:{ type:'loop' } },
        { from:'q1', to:'q0', label:'b', ...edgeDefaults },
        { from:'q1', to:'q1', label:'a', ...edgeDefaults, smooth:{ type:'loop' } },
        { from:'q0', to:'qa', label:'end(q0)', ...edgeDefaults },
    ];
    if (tmNetwork) tmNetwork.destroy();
    tmNetwork = new vis.Network(container,
        { nodes: new vis.DataSet(nodes), edges: new vis.DataSet(edges) },
        graphOptions
    );
}

// ── NFA ───────────────────────────────────────────────────────
function simulateNFA() {
    const input = document.getElementById('nfaInput').value.trim() || 'aab';
    let cur = new Set(['q0']);
    for (const sym of input) {
        const next = new Set();
        for (const s of cur) {
            if (s === 'q0' && sym === 'a') { next.add('q0'); next.add('q1'); }
            if (s === 'q0' && sym === 'b') next.add('q0');
            if (s === 'q1' && sym === 'b') next.add('q2');
        }
        cur = next;
    }
    document.getElementById('nfaCurrentStates').innerText = `{${[...cur].join(', ')}}`;
    const r = document.getElementById('nfaResult');
    r.className = `phase-result ${cur.has('q2') ? 'accept' : 'reject'}`;
    r.innerText = cur.has('q2')
        ? '✓ ACCEPTED — Reached q2'
        : '✗ REJECTED — No path to accept state';
}

function resetNFA() {
    document.getElementById('nfaInput').value = 'aab';
    document.getElementById('nfaCurrentStates').innerText = '{q0}';
    const r = document.getElementById('nfaResult');
    r.className = 'phase-result'; r.innerText = 'Ready';
}

// ── CFG ───────────────────────────────────────────────────────
function parseCFG() {
    const input = document.getElementById('cfgInput').value.trim();
    const r = document.getElementById('cfgResult');
    if (!input) { r.className='phase-result reject'; r.innerText='✗ Empty input'; return; }
    const a = (input.match(/a/g)||[]).length;
    const b = (input.match(/b/g)||[]).length;
    const balanced = /^a*b*$/.test(input) && a === b && a > 0;
    if (balanced) {
        r.className = 'phase-result accept';
        r.innerText = `✓ ACCEPTED — a^${a} b^${b} matches grammar`;
        document.getElementById('cfgDerivation').innerText = genTree(input);
    } else {
        r.className = 'phase-result reject';
        r.innerText = `✗ REJECTED — (${a} a's, ${b} b's) — not a^n b^n`;
    }
}

function genTree(str) {
    let t = 'S\n';
    for (let i = 0; i < str.length; i++)
        t += `${i < str.length-1 ? '├' : '└'}─ ${str[i]}\n`;
    return t;
}

function resetCFG() {
    document.getElementById('cfgInput').value = 'aabb';
    document.getElementById('cfgResult').className = 'phase-result';
    document.getElementById('cfgResult').innerText = 'Ready for input';
}

// ── KEYBOARD ─────────────────────────────────────────────────
document.addEventListener('keydown', e => {
    const btns = document.querySelectorAll('.tab-btn');
    const map  = {'1':'interactive','2':'phase2','3':'phase3','4':'phase4','5':'phase5','6':'phase6'};
    if (map[e.key]) switchTab(map[e.key], btns[+e.key - 1]);
    if (e.key === 'r' || e.key === 'R') onResetTransition();
});

// ── INIT ─────────────────────────────────────────────────────
window.onload = () => {
    buildMainGraph();
    drawTMGraph('q0');
    syncTable();
    updateStats();
};
</script>
</body>
</html>
